{# IronLung 3 - Intro Email Template

   Variables available:
   - prospect.first_name
   - prospect.last_name
   - prospect.title
   - company.name
   - company.loan_types (JSON array)
   - company.state
   - sender.name
   - sender.title
   - sender.company
#}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
</head>
<body style="font-family: Arial, sans-serif; font-size: 14px; color: #333;">
    <p>Hi {{ prospect.first_name }},</p>

    <p>I'm reaching out because I work with companies like {{ company.name }}
    in the private lending space.</p>

    <p>We help lenders streamline their loan origination process, and I thought
    it might be worth a quick conversation to see if there's a fit.</p>

    <p>Would you have 15 minutes this week or next for a brief call?</p>

    <p>Best,<br>
    {{ sender.name }}<br>
    {{ sender.title }}<br>
    {{ sender.company }}</p>
</body>
</html>
